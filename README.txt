datapath.sv is the main file.
testbench.sv is for the datapath.sv.
the *.mem files are the weights for corresponding architectures - weights_M_N.mem, M being # of layers and N # of neurons per layer.